apiVersion: v1
kind: Pod
metadata:
  name: kaniko-build
spec:
  containers:
  - name: kaniko
    image: gcr.io/kaniko-project/executor:latest
    args: 
    - "--context=git://github.com/zetamin/ubuntu.git"
    - "--dockerfile=Dockerfile"
    - "--no-push"
  restartPolicy: Never
